<?xml version="1.0"?>
<!-- <%= title %> <%= timestamp %> -->
<gpx version="1.1" creator="Xcode">
  <% points.forEach(function(point) { %>
    <wpt lat="<%= point.lat %>" lon="<%= point.lng %>" marked="<%= point.marked %>" />
  <% }); %>
</gpx>
